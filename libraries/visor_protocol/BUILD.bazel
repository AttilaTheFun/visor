load("@rules_swift//swift:swift.bzl", "swift_library")

package(default_visibility = ["//visibility:public"])

# The wire format and its JSON: no Foundation, so it is the same on every host.
swift_library(
    name = "visor_protocol",
    srcs = glob(["Sources/*.swift"]),
    module_name = "VisorProtocol",
)
