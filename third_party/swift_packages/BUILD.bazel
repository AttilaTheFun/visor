# The SwiftPM graph rules_swift_package_manager resolves (MODULE.bazel's
# swift_deps.from_package). Re-resolve after editing Package.swift:
# `cd third_party/swift_packages && swift package resolve && rm -rf .build`.
exports_files([
    "Package.swift",
    "Package.resolved",
])
